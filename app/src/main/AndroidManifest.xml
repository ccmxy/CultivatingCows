<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cultivatingcows">

    <!-- package="com.google.android.gms.location.sample.locationaddress" > -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COURSE_LOCATION" />

    <application
        android:name=".ParseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.RegisterLoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.YourGamesActivity"
            android:label="@string/title_activity_your_games" />
        <activity
            android:name=".UI.GameHomeActivity"
            android:label="@string/title_activity_game_home" />
        <activity
            android:name=".UI.GamePageActivity"
            android:label="@string/title_activity_game_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.YourCardsActivity"
            android:label="@string/title_activity_your_cards"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".UI.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".UI.Main2Activity"
            android:label="@string/title_activity_main2" />
        <activity
            android:name=".UI.YourMapsActivity"
            android:label="@string/title_activity_your_maps"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.AddMapPageActivity"
            android:label="@string/title_activity_add_map_page"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
